{
  "name": "lang-portal",
  "private": true,
  "workspaces": [
    "shared",
    "games/*"
  ],
  "scripts": {
    "build:shared": "yarn workspace @lang-portal/shared build",
    "build:games": "yarn workspaces foreach -p run build && node scripts/build-games.js",
    "dev:games": "yarn build:shared && yarn build:games"
  }
} 